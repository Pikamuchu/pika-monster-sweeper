import preferences from '../preferences';

const SWEEPER_LIMIT = 893; // Excluding monsters 100xx (No images available)

export const getSweepers = async (query = { userId: null }) => {
  // TODO: implement user sweeper info
  return null;
};

export const updateSweepers = async (query = { id: null, ids: [], userId: null }) => {
  // TODO: implement user sweeper update
};

export const deleteSweepers = async (query = { id: null, ids: [], userId: null }) => {
  // TODO: implement user sweeper delete
};
